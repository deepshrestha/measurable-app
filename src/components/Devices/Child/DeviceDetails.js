import React from "react";

const DeviceDetails = (props) => {
  console.log(props);
  return (
    <div className="user-dashboard">
      <div id="filter-card">Device Details</div>
    </div>
  );
};

export default DeviceDetails;
